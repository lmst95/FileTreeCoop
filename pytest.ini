[pytest]
# ``pytest -q`` im Wurzelverzeichnis meint die Server-Tests. Der Desktop-Client
# ist ein eigenes Programm mit eigenen Abhängigkeiten und bringt seine Tests
# selbst mit – die laufen mit ``cd client && pytest``.
testpaths = tests
